import React from "react";

const Playlist = () => {
    return (
        <section id="playlist" className="content">
            <h1>Playlist</h1>
        </section>
    );
};

export default Playlist;
